# Evaluators — Platform B1 MVP2

This folder will later hold local evaluator logic.

Do not build evaluator code until the registry, schemas, and mock strategy are committed.

Evaluator logic must remain separate from Platform B v1.
