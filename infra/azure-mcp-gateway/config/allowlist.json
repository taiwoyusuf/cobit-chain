{
  "version": "r1",
  "default": "deny",
  "tools": [
    "subscription_list",
    "group_list"
  ],
  "candidate_expansion_after_catalog_reconciliation": [
    "group_resource_list"
  ],
  "notes": [
    "R1 starts with the smallest currently verified read-only inventory surface.",
    "Candidate tools are not executable until deliberately moved into tools after live catalog and annotation reconciliation.",
    "No wildcard entries are permitted.",
    "Any unmatched tool is denied.",
    "Credential, secret, key, RBAC, write, action, create, update and delete operations are prohibited independently of this list."
  ]
}
